
(library
  (name calculator)
  (public_name calculator)
  (modules lexer parser display)
  (flags (:standard -warn-error +a-52))
  (libraries)
)

(executable
  (name calc)
  (public_name calc)
  (libraries calculator)
  (modules calc)
)
