
(library
  (name calculator)
  (public_name calculator)
  (modules calc_lexer calc_parser)
  (flags (:standard -warn-error +a-52))
  (libraries)
)

(executable
  (name calc)
  (public_name calc)
  (libraries calculator)
  (modules calc)
)
